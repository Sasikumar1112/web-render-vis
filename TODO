[~]python code to convert index html to string used import for that js react is enough
[]tokenizer use string object difference to construct a tree like DS
[~]display the Html on the side and traverse for loop and blink the character



Last
[]CSS and animation libs like animateUI